More Circuits：
    Cellular automata：
        【TODO】 这部分内容有很大用途/应用场景，后续需要单独学习；
        CA 是由大量相同的基本处理单元（元胞）组成的阵列。每个单元只与其邻近单元通信，并根据局部规则更新自身状态，通常采用时钟同步的并行方式工作；
        应用形式：
            片上伪随机数生成器 (PRNG) 与加密；
            硬件加速器 / 专用计算阵列；
            片上存储与多端口寄存器堆；
            片上网络 (NoC) 的简化模型；
            故障容错与缺陷容忍