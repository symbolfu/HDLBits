Finite State Machines：
    coding风格：
        一段式状态机：
            所有逻辑（状态转移、输出、状态寄存）都写在一个always块中。
            参考一段式状态机参考代码；
            缺点：
                代码冗长，不易维护，输出寄存需额外处理。

        两段式状态机：
            状态转移和状态寄存在一个always块，
            输出逻辑在另一个always块或assign语句。
            优点：
                结构清晰；缺点：组合逻辑输出可能有毛刺。
                毛刺产生原因：
                    其输出信号是用组合逻辑直接对当前状态进行译码得到的。
                    current_state 是由 时钟沿触发的寄存器输出，理论上变化是同步的；
                    但寄存器输出并不是“理想瞬间跳变”的。实际电路中：
                        由于多个触发器输出到达时间不同（skew）
                        时钟到Q的延迟不同：指的是时钟到达CLK port后data从Q端输出的延迟；
                        信号走线延迟不同
                        current_state 的多位状态编码（如 3'b001 -> 3'b010）不可能同时翻转；
                毛刺问题后端工具不能解决？
                    只要输出是“当前状态的组合逻辑”，状态跳变时必然有短暂的不稳定期。
                    skew：
                        时钟树，能减小因时钟到达时间不同导致的毛刺，但不能消除 Tckq 差异；
                            Tckq：
                                时钟有效沿到来之后，触发器输出端（Q）需要多长时间才能稳定地反映出输入（D）的值。
                        组合逻辑优化，只能让毛刺更窄，无法消除；

        三段式状态机：最推荐使用
            状态转移、输出逻辑、状态寄存分别三个always块，输出采用寄存器输出。
            优点：
                无毛刺，时序清晰，易于综合和调试，最推荐使用。

        状态编码风格：
            二进制编码（Binary）：
                节省触发器，但组合逻辑复杂。
            格雷码（Gray）：
                相邻状态只变1位，减少功耗和毛刺，适合异步FIFO。
            独热码（One-hot）：
                每个状态一个触发器，译码快，面积稍大，FPGA常用。