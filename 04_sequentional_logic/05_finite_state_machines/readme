Finite State Machines：
    coding风格：
        一段式状态机：
            所有逻辑（状态转移、输出、状态寄存）都写在一个always块中。
            参考一段式状态机参考代码；
            缺点：
                代码冗长，不易维护，输出寄存需额外处理。

        两段式状态机：
            状态转移和状态寄存在一个always块，
            输出逻辑在另一个always块或assign语句。
            优点：
                结构清晰；缺点：组合逻辑输出可能有毛刺。
                毛刺产生原因：
                    其输出信号是用组合逻辑直接对当前状态进行译码得到的。
                    current_state 是由 时钟沿触发的寄存器输出，理论上变化是同步的；
                    但寄存器输出并不是“理想瞬间跳变”的。实际电路中：
                        由于多个触发器输出到达时间不同（skew）
                        时钟到Q的延迟不同：指的是时钟到达CLK port后data从Q端输出的延迟；
                        信号走线延迟不同
                        current_state 的多位状态编码（如 3'b001 -> 3'b010）不可能同时翻转；
                毛刺问题后端工具不能解决？
                    只要输出是“当前状态的组合逻辑”，状态跳变时必然有短暂的不稳定期。
                    skew：
                        时钟树，能减小因时钟到达时间不同导致的毛刺，但不能消除 Tckq 差异；
                            Tckq：
                                时钟有效沿到来之后，触发器输出端（Q）需要多长时间才能稳定地反映出输入（D）的值。
                        组合逻辑优化，只能让毛刺更窄，无法消除；

        三段式状态机：最推荐使用
            状态转移、输出逻辑、状态寄存分别三个always块，输出采用寄存器输出。
            优点：
                无毛刺，时序清晰，易于综合和调试，最推荐使用。

        状态编码风格：
            二进制编码（Binary）：
                节省触发器，但组合逻辑复杂。
            格雷码（Gray）：
                相邻状态只变1位，减少功耗和毛刺，适合异步FIFO。
            独热码（One-hot）：
                每个状态一个触发器，译码快，面积稍大，FPGA常用。


    状态机类型：
        moore类型状态机：
            特点：
                输出只取决于当前状态，而与输入无关；
                更可预测（输出与时钟同步）
                没有组合逻辑直接输出路径（某些设计中更安全）
                缺点：  
                    可能比Mealy型需要更多状态
                    输出只在时钟边沿变化，响应可能慢一拍
        Mealy型状态机：
            特点：
                输出不仅取决于当前状态，还取决于当前输入；
                通常需要更少的状态（电路面积更小）
                应更快——输入变化后当前周期就能得到输出，不需要等下一个时钟沿；
                对于某些高速应用更合适
                缺点：  
                    输出可能有毛刺（因为输出是组合逻辑）
                    时序分析更复杂
                    如果设计不当，可能产生竞争风险；


    独热码（Onehot)编码：
        恰好只有一个状态位为 1，其余全为 0。
        优势、特点：
            只需检查一个状态位；
            可以减少比较器的使用；
            只需一根导线（对应 S2 的触发器输出），不需要比较器。
            状态检测逻辑从“多输入比较器”退化为“单根信号线”

