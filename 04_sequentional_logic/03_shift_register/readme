shift registers：
    4-bit shift register
    rotate100
    Build a 64-bit arithmetic shift register
        sign bit of the number
        An arithmetic right shift shifts： Arithmetic Right Shift（算术右移）
            移位后，最高位用原来的符号位填充（正数补0，负数补1）
        a logical right shift：Logical Right Shift（逻辑右移）
            移位后，最高位始终补0
        算术左移和逻辑左移是完全一样的
        
        为什么有这样的差异呢：
            右移的本质是除法，左移的本质是乘法；
                除法（对应右移） 需要处理 符号 和 取整方向（算术右移规定：向下取整）；
                乘法（对应左移） 不需要处理取整，也不会改变符号规则，如果符号位变化，表示溢出；
    
    lfsr5：线性反馈移位寄存器
        A linear feedback shift register；
        它是数字电路和密码学中非常重要的一种结构；
        基本概念：
            LFSR 由两部分组成：
                移位寄存器；
                    一串寄存器（比如 8 个 bit），每个时钟周期将数据向右或向左移动一位；
                线性反馈函数；
                    将寄存器中某些特定的位置（抽头） 的值取出，进行 异或（XOR） 运算，
                    然后将结果反馈回移位寄存器空出来的那一位；
                    最后反馈的q与“插入位置的q”做异或，然后给d端；
                特性：
                    伪随机性：
                        如果抽头选择得当，输出的 bit 序列看起来完全是随机的，实际上由初始值（种子）决定；
                    最大周期：
                        2^n - 1
                    线性：
                        反馈函数只用 XOR（本质是 GF(2) 上的加法）
                        不适合做高强度的密码学；
            应用：
                CRC（循环冗余校验）：计算网络数据包的校验和。
                伪随机数生成器：用于游戏、蒙特卡洛模拟（但不用于安全加密）。
                通信：CDMA 手机通信、GPS 信号、蓝牙中的扩频码。
                内置自测：芯片出厂时，用它生成测试向量检查自身逻辑。

            案例：
                3-bit LFSR, 抽头在b2和b0， 多项式位x^3 + x + 1, 为什么，这个多项式的含义是什么?
                规则：
                    对于 n 位 LFSR，我们通常用一个 n 次本原多项式 来描述反馈抽头；
                    Fibonacci LFSR 的标准右移结构：
                        x^n：对应 反馈输入端（总是存在，不参与抽头 XOR 判断）
                    Galois LFSR 结构：
                        x^n：对应 反馈输入端（总是存在，参与抽头 XOR 判断）
                    其他item项：如果存在则表示参与抽头；
                    1： x⁰ (=1) 对应抽头 b0 的参与；
                    缺失项表示，不参与抽头；

    这个章节练习了很多lsfr电路图，但是具体应用场景还未了解；

    a 3-input look-up-table (LUT):
        an 8x1 memory
         extend the circuit to have 3 additional inputs A,B,C and an output Z. 
         The circuit's behaviour should be as follows: when ABC is 000, Z=Q[0], when ABC is 001, Z=Q[1], and so on.