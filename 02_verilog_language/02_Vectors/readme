vectors part：
    to group related signals using one name to make it more convenient to manipulate;
    声明方式：
        the declaration of a vector places the dimensions before the name of the vector；
        wire [7:0] w; declares an 8-bit vector named w that is functionally equivalent to having 8 separate wires;
        语法：
            type [upper:lower] vector_name;
            声明时只能先upper，后lower，也就像little-endian


Implicit nets：
    In Verilog, net-type signals can be implicitly created by an assign statement or by attaching something undeclared to a module port.
    规避隐式声明：存在bug的可能性
        Disabling creation of implicit nets can be done using the `default_nettype none directive.

Unpacked vs. Packed Arrays：
    packed Arrays：
        the vector indices are written before the vector name
        上面type [upper:lower] vector_name 格式的声明就是packed dimensions of the array；
        where the bits are "packed" together into a blob (this is relevant in a simulator, but not in hardware)
    unpacked Arrays：
        The unpacked dimensions are declared after the name；
        应用场景：
            They are generally used to declare memory arrays；
        reg [7:0] mem [255:0];   
        reg mem2 [28:0];
            在行业中，对于变量名右侧的unpacked维度定义应该是[low:high]格式

    
Accessing Vector Elements: Part-Select
    对于一定位宽的vector（a set of signals），如何获取其中部分的signals；
    类似于高级语言的数组索引或者切片；
    位宽不匹配时的处理：
        If the lengths of the right and left sides don't match, it is zero-extended or truncated as appropriate.
        0扩展或者截断处理；

    从物理电路图上理解下面表达式的区别：
        out[0:7] = in[7:0];
        out[7:0] = in[7:0];

Bitwise vs. Logical Operators:
    When using vectors, the distinction between the two operator types becomes important.
    rule:
        A bitwise operation between two N-bit vectors replicates the operation for each bit of the vector and produces a N-bit output;
         a logical operation treats the entire vector as a boolean value (true = non-zero, false = zero) and produces a 1-bit output.


concatenation operator:
    part-select的反向操作；
    part-select是从vector中取部分信号；
    而concatenation operator是将多个信号或者信号集（vector）形成新的vector；
    语法：
        {a, b},  而且如果a和b为常数，必须显示标注位宽
        unsized constants are not allowed in concatenations.
        {3'b111, 3'b000} => 6'b111000

    concatenation场景：
        want the same thing concatenated together many times：
            原来的方式也可行，但是过于繁琐；
            verilog提供的语法便利：{5{1'b1}}
            {3'd5, {2{3'd6}}}


可综合的for语句：
    模版：
        integer i;  // 循环变量必须声明为整数
        always @(*) begin
            for (i = 0; i < 常量; i = i + 1) begin
                // 循环体 - 每次迭代生成独立硬件
            end
        end

    应用场景与限制：
        前提是循环次数必须在综合时为常数，用 parameter 或 localparam 控制循环次数，生成不同规模的电路；
        重复的同构逻辑生成：加法器、比较器、多路选择器。
        优先编码器：
            遍历信号位，找到第一个有效位（如仲裁逻辑）
                for (i = 0; i < N; i = i + 1)
                    if (request[i] && !grant_any)
                        grant = i;
                实际电路图可能是：req每个bit信号与grant_any取反后做逻辑与运算，然后再通过一个优先级编码器输出给grant信号；
        循环变量（如 i）不能在循环体内被修改（综合工具要求静态展开）；
        避免在 always @(posedge clk) 中使用 for 生成时序依赖复杂的状态机，否则可能综合出大量硬件；
        
    建议：
        对大规模重复模块，优先使用 generate + for（Verilog-2001 支持）代替普通 always 块中的 for，以获得更清晰的层次结构；
        







