HDLBits网页的练习记录：
这个repo用于verilog练习 
note：
    github无法正常提交fix方法：
        场景：开启vpu也无法提交：
            执行下面命令：设置 Git 全局代理
                git config --global http.proxy http://127.0.0.1:7890
                git config --global https.proxy http://127.0.0.1:7890
            取消代理命令：
                git config --global --unset http.proxy
                git config --global --unset https.proxy

topics：
    geting started              done
    verilog language
        basics
        vectors
        modules: hierachy
        procedures
        more verilog features
    
    Circuits:
        combinational logic
        sequential logic
        building larger curcuits

    verification:
        reading simulation
        writing testbenches



github  name rename to test

除了verification部分，都已经完成了！
但是在学习过程中，对状态机还是欠缺，后续可以开始以ip为单位进行研究！